{% import "components/filetree.njk" as FileTree %}
<div>
  {% set items = filetree %}
  {% for key, node in items %}
    <div>
      <h3>{{ key }}</h3>
      {{ FileTree.menuItem(key, node, 0, key) }}
    </div>
  {% endfor %}
</div>
